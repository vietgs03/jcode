interface:
  display_name: "Jcode"
  short_description: "Repo-specific patterns and workflows for jcode"
  default_prompt: "Use the jcode repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true